# Extra autostart processes
# exec-once = uwsm app -- my-service

# Load hyprpm Plugins at autostart
exec-once = hyprpm reload -n

#netbird autostart
exec-once = netbird up --management-url https://netbird.detailm.cloud:443